{
  "name": "mcp-json-scada-db",
  "version": "0.1.0",
  "description": "{json:scada} - MCP Server for JSON-SCADA MongoDB.",
  "type": "module",
  "main": "dist/mcp-server.js",
  "bin": {
    "mcp-json-scada-db": "./dist/mcp-server.js"
  },
  "config": {
    "envPrefix": "JS_MCPJSDB_"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/mcp-server.js",
    "dev": "tsx src/mcp-server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist"
  },
  "author": "Ricardo Lastra Olsen",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "mongodb": "^7.0.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/mongodb": "^4.0.6",
    "@types/node": "^25.0.9",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  }
}
